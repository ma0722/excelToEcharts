1.数据表的名称为data.xlsx，位于excel下。
2.所有的表格都位于数据表data.xlsx内，以4张sheet形式出现，名称为DC1-TG现状配置表,DC1-SS现状配置表, DC1-TG扩容需求表, DC1-SS扩容需求表。
3.每张表格的起始位置为A1处。
4.两张现状配置表的横坐标不可以扩展，纵坐标可以扩展。
5.两张扩容需求表的横坐标可以扩展（仅限于关于年份），纵坐标可以扩展。
6.表格到结构不要改变。